const numero1 = 20;
const numero2 = "20";

// Esto es una función
console.log(parseInt(numero2));

// Esto es un método
console.log(numero1.toString());

/*
Puedes ver que mientras la función toma el valor en el paréntesis, el método añade un punto seguido del nombre.
Hora ese valor que se pasa en el paréntesis, se llaman argumentos de la función.
*/


