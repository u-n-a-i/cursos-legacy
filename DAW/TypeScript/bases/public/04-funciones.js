"use strict";
// Funciones
function sum(n1, n2) {
    return n1 + n2;
}
let resultado = sum(2, 2);
console.log(resultado);
const toUpper = (str) => {
    return str.toUpperCase();
};
console.log(toUpper("hola desde ts"));
